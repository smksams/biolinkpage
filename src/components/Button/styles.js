import styled from "styled-components";

export const Container = styled.button`
  background-color: ${({theme}) => theme.COLORS.ROSA_PASTEL};
  padding: 1rem 3rem;
  align-items: center;
  border-radius: 1rem;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: .8rem 0;
  border-radius: 4rem;
  font-size: 2rem;
`